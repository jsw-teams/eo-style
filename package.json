{
  "name": "eo-pages-static-lib-mirror",
  "version": "1.2.0",
  "private": true,
  "description": "Pure static frontend library mirror for EO Pages",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "sync:libs": "node scripts/sync-libs.mjs",
    "sync:build": "npm run sync:libs && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}